{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{% trans %}company.edit{%endtrans%}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('company') }}">
            {% trans %}action.back_to_list{%endtrans%}
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
